/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { MeusPetsAdicionarComponent } from './meus-pets-adicionar.component';

describe('MeusPetsAdicionarComponent', () => {
  let component: MeusPetsAdicionarComponent;
  let fixture: ComponentFixture<MeusPetsAdicionarComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [MeusPetsAdicionarComponent]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(MeusPetsAdicionarComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */